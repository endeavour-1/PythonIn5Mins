# Tuples are kind of like Lists but Are Immutable ~ ` We cannot change them once made`

numbers = (1,2,3)
# Only this Methods work.
# numbers.count()
# numbers.index()

# If We don't want to change any element then we use Tuple Instead of List.

# DONE !